apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-settings
data:
  HF_REPO: "<your-user>/confidential-ml-artifacts"
  # Bootstrap fills the full artifact commit returned by Producer before Consumer.
  HF_REVISION: ""
